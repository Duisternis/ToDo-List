import React from 'react';

import Heading from './Heading';

const Stats = (props) => {
    return (
        <div className={props.className}>
            <Heading />
        </div>
    );
};

export default Stats;